# 0814 第二轮页面与数据修复报告

## 备份与基线

- 项目备份：`backup/0814-second-fix-before-20260814-192329.zip`。
- 数据库备份：`backup/commercial_complex_carbon_db_before_0814_second_fix.sql`。
- 修复前记录数：综合体 3、建筑 4、功能区域 13、计量节点 29、计量设备 222、能耗记录 5502、碳核算记录 5502、操作日志 508。
- 未重新执行主 SQL，未 drop 数据库，未删除原有能耗、碳核算或其他有效模拟数据。

## 欢迎页与登录页

- 欢迎页删除“数据库应用课程设计 · 商业综合体低碳运营场景”底部说明。
- 登录页删除公开演示账号说明，并移除用户名和密码输入框的预填值。
- BCrypt 登录接口和失败提示保持不变。

## 综合体与建筑数据

- 异常综合体的真实原因是功能验收阶段写入了 `ACC-` 编码且中文值已永久保存为问号，并非表格列映射错误。
- `ACC-0811220309` 记录保留并明确标记为“功能验收测试综合体”，继续保持停用。
- 宁波示范综合体保留，修正编码、城市、地址、面积和运营资料并启用。
- 建筑表原有 4 条，其中杭州仅 2 条启用，另有 1 条停用设备楼和 1 条乱码验收测试楼。
- 新增 10 条幂等示范建筑：杭州 6 条、宁波 4 条。修复后杭州 8 条启用建筑，宁波 4 条启用建筑。
- 综合体列表继续使用明确中文列定义；总建筑面积以 `m²` 展示，顶部选择器只加载启用综合体。

## 计量体系乱码

- 数据库中 27、28、29 号计量节点及 `AA-0811220309` 停用测试区域的名称确实以 `3F` 字节保存为问号，单纯修改页面编码不能恢复。
- 根据业务编码、状态和验收来源，将停用测试节点与测试区域修正为明确中文名称；修复后计量节点和功能区域问号记录均为 0。
- JDBC、JSP、Servlet/JSON 和 Maven 均使用 UTF-8；Servlet 在读取请求前显式设置 UTF-8。
- 节点类型继续由前端映射为综合体、建筑、功能区域、商户、设备或未知类型。

## 图表与资源

- 雷达图旧问题来自停用乱码区域仍进入查询、重复的历史配置以及图例/多选区域共用顶部空间；后端现只查询启用建筑、区域、节点和设备，前端使用滚动图例、居中雷达主体、0—100 绩效标准化和原始值 Tooltip。
- 仪表盘使用单一 `detail` 百分比、半圆布局和三列辅助指标，避免重复文本。
- 读取 `图片/logo.png` 作为 `assets/images/brand-logo.png`。
- 读取浅色 `图片/背景.png` 作为 `assets/images/app-background.png`；保留原欢迎页 `landing-background.png`。
- 登录后全部模块共享主内容背景，卡片、表格和输入框保持清晰白色表面。
- 左侧旧品牌文字视觉隐藏，改用用户 Logo；点击 Logo 返回总览，图片加载失败时显示“低碳管理”。
- 浏览器回归发现外部 CDN 偶发不可用会导致六张图表空白，因此通过 Maven 获取 ECharts 5.5.1 WebJar，并将 `echarts.min.js` 固定到 `assets/vendor`，页面不再依赖外部 CDN。

## 增量 SQL

- `database/patches/030_clean_acceptance_test_data.sql`
- `database/patches/031_expand_demo_buildings.sql`
- `database/patches/032_fix_garbled_demo_labels.sql`

三个补丁均使用稳定业务编码判定，可重复执行，不修改数据库结构。

## 验收状态

- `mvnw.cmd test`：BUILD SUCCESS，26 项测试全部通过。
- `mvnw.cmd clean package`：BUILD SUCCESS，最终 WAR 大小 11,282,597 字节。
- 最新 WAR 已清理旧应用目录后重新部署，Tomcat 返回 HTTP 200；本地 ECharts、Logo、背景图片均返回 HTTP 200。
- 真实 API 登录成功；杭州/宁波建筑分别返回 8/4 条；计量树和雷达结果无问号；雷达只返回 8 个启用区域；预算执行率接口返回 11.210534%，页面按两位小数显示。
- 修复后记录数：综合体 3、建筑 14、功能区域 13、计量节点 29、计量设备 222、能耗记录 5502、碳核算记录 5502、操作日志 508。
- 浏览器工具前序实测确认欢迎页和登录页说明已清除、Logo 和背景已加载、导航及页面无 JavaScript error；随后发现 CDN 图表空白并改为本地 ECharts。
- 指定的 Chrome 无痕最终截图尚未执行：当前 Codex 环境未连接 Chrome 扩展，按浏览器安全规则不能改用其他自动化方式代替 Chrome。现有旧截图未冒充本轮截图，要求的六个 `0814-second-*` 文件未伪造。
