# 用户可见测试文字清理报告

## 清理策略

- 不删除有效业务记录；有关联的验收记录保留并停用。
- 只修改用户可见编码、名称、标题、内容、备注、审计说明和 JSON 展示快照。
- 技术文档继续如实说明数据由程序生成。

## 清理内容

- 220 台早期设备：`demo-mtr-*` 改为正式中性编码，备注改为“在用计量设备”。
- 250 条早期预警：重写编码、中文业务标题、事件内容和备注。
- 250 条早期整改：重写编码、标题、内容、措施、结果和备注，并修复问号乱码。
- 500 条早期操作日志：对象类型、对象编号、说明和请求地址改为正常审计表达。
- 建筑、区域、商户、入驻、节能项目、评价、分摊规则和账单备注统一改为业务表述。
- 一条 `ACC-E-*` 能耗记录保留关联关系，改为历史中性编码并修正乱码原因和 JSON 来源。
- Dashboard“累计模拟减排量”改为“累计减排量”；月报保留非正式碳核证边界，不显示课程测试措辞。

## 操作日志页面

- 隐藏“新增”按钮，日志不可手工创建或编辑。
- `insert/update/select/delete/login/logout/audit/export/generate` 映射为新增、修改、查询、删除、登录、退出、审核、导出、生成。
- 新增模块、操作类型、用户、开始日期、结束日期和关键词筛选。
- 默认按操作时间倒序，支持按筛选条件分页拉取并导出完整 CSV。

## 验证结果

- `database/verify_no_user_visible_test_text.sql`：17 个业务范围剩余数均为 0。
- `scripts/verify-user-visible-text.ps1`：源码用户可见痕迹 0，最终 WAR 用户可见痕迹 0。
- 扫描范围包括 JSP、JavaScript、Java、HTML、CSS 和 WAR 中可见资源；本地 ECharts vendor 文件排除。

