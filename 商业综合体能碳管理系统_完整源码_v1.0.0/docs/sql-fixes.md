# SQL 修复记录

未直接覆盖原始 `2025333541001戴哲语.sql`。静态检查确认对象名称与课程要求一致，未擅自改写表、视图、函数、触发器或存储过程。公开仓库不再包含任何管理员密码种子；云端管理员由环境变量和 BCrypt 首次初始化。

导入前建议使用 MySQL 8：

```text
mysql --default-character-set=utf8mb4 -uroot -p < 2025333541001戴哲语.sql
```

如 MySQL 报告特定版本兼容错误，请保留原始脚本，将最小修复追加到 `database/patches/` 并记录执行顺序。
