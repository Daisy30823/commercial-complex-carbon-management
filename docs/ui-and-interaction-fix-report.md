# 界面与交互整改报告

## 基线与约束

- 2026-08-13 已备份可运行项目：`backup/ui-issues-before-20260813-230603.zip`。
- 已使用 `mysqldump` 备份数据库：`backup/commercial_complex_carbon_db_before_ui_fix.sql`。
- 本轮未执行主 SQL、未删除数据库或模拟数据，未修改数据库结构和业务过程。

## 问题整改与验收

| 问题 | 原因 | 修改方案与文件 | 实际结果 |
|---|---|---|---|
| 刷新回总览 | 单页状态只保存在内存 | `app.js` 使用 URL Hash、`pushState`、`popstate` | 商户页刷新仍为 `#/merchants`，前进后退可用，通过 |
| 综合体上下文 | API 未统一带上下文 | `ApiServlet.java` 列表、摘要、图表增加 `complexId` 过滤；顶部选择器写入 `sessionStorage` | 综合体 1 返回 29 个节点，综合体 4 返回 0 个节点，隔离有效，通过 |
| 统计日期 | 顶部日期为静态文本 | `app.jsp`/`app.js` 改为 date input，保存 `statDate`，摘要按结束日期查询 | 切换日期后请求参数变化且页面不跳路由，通过 |
| 取消按钮 | 表单关闭逻辑分散 | 统一 `dialog` 关闭、Esc 和取消处理 | 综合体编辑取消可关闭且不提交，通过 |
| 表单排版 | 字段横向挤压 | `app.css` 统一两列 Grid、窄屏单列、滚动弹窗 | 桌面端两列、窄屏单列，通过 |
| 指标卡下钻 | 卡片无交互 | KPI 增加 `data-drill` 路由 | 点击进入对应业务模块，通过 |
| 图表说明占空间 | 说明固定展开 | 统一 `?` 按钮和帮助弹窗 | 默认不占空间，说明仍可查看，通过 |
| 区域排名难读 | 使用纵向类别轴 | 横向柱状图、截断标签、右侧数值 | 区域名水平显示，单位为 tCO2e，通过 |
| 热力图裁切 | visualMap 位于卡片外 | visualMap 底部横向、containLabel 和 resize | 1366/1920 布局不溢出，通过 |
| 雷达图交互 | 区域不可选择 | 保留数据库雷达结果并增强为多选控件，默认前三个 | 控件支持 2—5 个区域选择，数据来自当前综合体，通过 |
| 预算仪表盘 | 刻度拥挤 | 半圆 gauge、执行率中心显示、预算摘要 | 显示实际、预算、剩余值，通过 |
| 英文化界面 | 直接使用字段名 | `app.js` 增加模块中文列定义和状态格式化 | 普通列表不再以字段名为主标题，通过 |
| 分页与按钮间距 | 操作按钮紧贴 | `app.css` 统一按钮间距和禁用态 | 分页和操作列可读，通过 |
| 计量节点树 | 页面信息密集 | `app.js` 递归树、搜索、展开/收起、详情面板；仍调用树过程 | 树刷新、搜索、节点详情和 CRUD 入口可用，通过 |
| 专题查询 | 结果直接展示 JSON | 查询 Tab、中文结果表格、空状态和 CSV 导出 | 七类查询入口保留，结果表格化，通过 |
| 个人中心 | 无用户页面 | `ApiServlet.java` 增加 profile、资料、密码和日志接口；`app.js` 增加页面 | 个人中心可打开，密码使用 BCrypt 接口，通过 |

## 构建与部署

- `mvnw.cmd test`：BUILD SUCCESS，3 个测试通过。
- `mvnw.cmd clean package`：BUILD SUCCESS。
- 最新 WAR：`<项目目录>\target\commercial-complex-carbon.war`。
- 已停止 Tomcat、清理旧 WAR/展开目录和 work 缓存，复制最新 WAR 并重新启动。
- 浏览器登录、路由刷新、综合体切换和页面访问已验证；控制台无 JavaScript error。

## 未完成项

- 当前环境未对所有弹窗模块逐一录制人工操作视频；后端既有 CRUD 接口和通用弹窗逻辑保持不变。
- ECharts 使用 CDN，离线环境需要预先缓存 vendor 资源；不影响联网本机演示。
