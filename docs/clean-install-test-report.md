# v1.0.0 从零安装测试报告

## 隔离策略

- 正式数据库：`commercial_complex_carbon_db`，未执行主 SQL、未 drop、未清空或重建。
- 临时数据库：`commercial_complex_carbon_release_test`。
- 安装 SQL：复制发布 SQL后，仅在临时副本中把数据库名替换为临时库名。
- 测试完成后已停止临时 Tomcat、删除临时数据库并恢复正式数据库连接。

## 安装与数据库验证

完整演示 SQL 从空环境导入成功。验证结果：29 张表、27 张表不少于 15 列、全部 29 张表有主键、76 个外键、3 个视图、16 个 JSON 字段、2 个自关联外键、43 个存储过程、3 个函数、8 个触发器、11 张表不少于 200 条记录、8 个启用综合体。

发布库不包含 `已移除的课程只读账号`、`已移除的无意义测试账号`、`已移除的私人测试账号` 三个后期私人/测试注册账号。课程管理员和正式演示运营账号保留。

## Java 集成测试

通过 `COMMERCIAL_DB_URL`、`COMMERCIAL_DB_USERNAME` 和 `COMMERCIAL_DB_PASSWORD` 环境变量让项目连接临时数据库，执行 `mvnw.cmd test`：46 项测试，0 失败、0 错误、0 跳过，BUILD SUCCESS。

## Tomcat 与 HTTP 验收

使用不含 `db.properties` 的最终 WAR，Tomcat 10.1 通过环境变量连接临时数据库。

| 验收项 | 实际结果 |
| --- | --- |
| 管理员登录 | 成功，当前用户接口返回 `admin` |
| Dashboard | 成功，杭州综合体建筑数量返回 13 |
| 专题查询 | 能源结构查询返回 5 行真实数据库结果 |
| 存储过程 CRUD | 综合体新增成功，修改名称成功，停用后 `record_status=0` |
| 能耗触发碳核算 | 能耗与碳记录均由 48,702 增至 48,703 |
| 核算公式 | 活动数据 10，因子快照 0.5366，结果 5.366 kgCO₂e，与乘积一致 |

## 清理与恢复

- `commercial_complex_carbon_release_test` 已删除，元数据查询结果为 0。
- 正式库核心数量复核：综合体 9、设备 462、能耗 48,702、碳核算 48,702、预警 838、整改任务 530。
- 正式系统重新启动后 HTTP 200，使用本机私密管理员凭据登录成功。
- 正式业务数据未因从零安装测试而减少或重建；操作日志会记录正常验收登录行为。
